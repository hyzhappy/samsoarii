<UserControl x:Class="SamSoarII.Shell.Windows.ProjectTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SamSoarII.Shell.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="300" Focusable="True"
             AllowDrop="True"
             MouseMove="OnPTVIMouseMove"
             DragOver="OnPTVIDragOver"
             DragLeave="TV_Main_DragLeave"
             Drop="TV_Main_Drop">
    <!--<UserControl.Resources>
        <Style TargetType="{x:Type local:ProjectTreeViewItem}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF7DBEF6"></Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>-->
    <ScrollViewer x:Uid="ScrollViewer_1" x:Name="Scroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <TreeView x:Uid="TV_Main" x:Name="TV_Main" FontSize="12" Focusable="True"/>
    </ScrollViewer>
</UserControl>
