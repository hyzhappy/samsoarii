<UserControl x:Class="SamSoarII.Shell.Models.LadderNetworkViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SamSoarII.Shell.Models"
             xmlns:mg="clr-namespace:SamSoarII.Shell.Managers"
             xmlns:gb="clr-namespace:SamSoarII.Global"
             xmlns:properties="clr-namespace:SamSoarII.Properties"
             xmlns:util="clr-namespace:SamSoarII.Utility;assembly=SamSoarII.Utility"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="{x:Static gb:GlobalSetting.WholeWidth}" x:Name="LadderNetworkUserControl"
             AllowDrop="True">
    <UserControl.Resources>
        <ObjectDataProvider x:Uid="ObjectDataProvider_1" x:Key="FontData" ObjectType="{x:Type mg:FontManager}" MethodName="GetTitle"/>
        <util:BoolToVisibilityConverter2 x:Uid="util:BoolToVisibilityConverter2_1" x:Key="converter"></util:BoolToVisibilityConverter2>
    </UserControl.Resources>
    <StackPanel x:Uid="StackPanel_1" Orientation="Horizontal">
        <local:LadderExpander x:Uid="ladderExpander" x:Name="ladderExpander" VerticalAlignment="Top" IsExpand="True" Margin="-50,0,0,0"/>
        <Grid x:Uid="Grid_1">
            <Rectangle x:Name="Rect" VerticalAlignment="Top"/>
            <StackPanel x:Name="mainstack" Margin="0,0,0,20">
                <StackPanel.Resources>
                    <ContextMenu x:Uid="ContextMenu_1" x:Key="CommentAreaContextMenu" DataContext="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget.Tag}">
                        <MenuItem x:Uid="MenuItem_1" Header="{x:Static properties:Resources.LadderNetwork_Edit_Network_Comment}"></MenuItem>
                    </ContextMenu>
                </StackPanel.Resources>
                <Grid x:Name="CommentAreaGrid" Background="Transparent">
                    <Border x:Name="CommentAreaBorder" BorderThickness="4" BorderBrush="Brown" Width="1500" Margin="20,0,0,0" HorizontalAlignment="Left">
                        <Expander x:Name="CommentAreaExpander" Width="1500" Background="LightCyan" MouseDoubleClick="OnEditComment"
                                  SizeChanged="CommentAreaExpander_SizeChanged">
                            <Expander.Header>
                                <Grid x:Uid="Grid_2" Width="1500" ContextMenu="{StaticResource CommentAreaContextMenu}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Uid="ColumnDefinition_1" Width="1*"></ColumnDefinition>
                                        <ColumnDefinition x:Uid="ColumnDefinition_2" Width="1*"></ColumnDefinition>
                                        <ColumnDefinition x:Uid="ColumnDefinition_3" Width="3*"></ColumnDefinition>
                                        <ColumnDefinition x:Uid="ColumnDefinition_4" Width="1*"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Label x:Uid="NetworkNumberLabel" x:Name="NetworkNumberLabel" DataContext="{StaticResource FontData}" FontSize="{Binding FontSize}" FontFamily="{Binding FontFamily}" Foreground="{Binding FontColor}" Grid.Column="0" Grid.Row="0"></Label>
                                    <Label x:Uid="NetworkStepCountLabel" x:Name="NetworkStepCountLabel" DataContext="{StaticResource FontData}" FontSize="{Binding FontSize}" FontFamily="{Binding FontFamily}" Foreground="{Binding FontColor}" Grid.Column="1" Grid.Row="0">Step:</Label>
                                    <Label x:Uid="NetworkBriefLabel" x:Name="NetworkBriefLabel" FontSize="32" Grid.Column="2" Grid.Row="0" Content="{Binding NetworkBrief}"></Label>
                                </Grid>
                            </Expander.Header>
                            <TextBlock x:Uid="NetworkDescriptionTextBlock" x:Name="NetworkDescriptionTextBlock" FontSize="32" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding NetworkDescription}"></TextBlock>
                        </Expander>
                    </Border>
                    <Button x:Name="ThumbnailButton" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="1540,0,0,0" Width="65" Height="55" 
                            Template="{StaticResource ThumbnailButton}"
                            ToolTipOpening="ThumbnailButton_ToolTipOpening"
                            ToolTipClosing="ThumbnailButton_ToolTipClosing"
                            MouseWheel="OnThumbnailButtonMouseWheel"
                            MouseDoubleClick="OnThumbnailButtonMouseDoubleClick"/>
                </Grid>
                <Grid x:Name="GD_Canvas">
                    <Canvas x:Name="LadderCanvas"
                        HorizontalAlignment="Left" Height="300" Width="3600" Margin="0,20,0,0" Background="Transparent" 
                        DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}">
                    </Canvas>
                </Grid>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
